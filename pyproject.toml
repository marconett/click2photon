[project]
name = "click2photon"
version = "0.1.0"
description = "End-to-end display latency measurement tool (photodiode + RP2040)"
requires-python = ">=3.13"
dependencies = [
    "prompt-toolkit>=3.0.52",
    "pyserial>=3.5",
]
